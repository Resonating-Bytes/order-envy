<script>
    function validateForm(passwordStr) {
        let newPassword = document.forms["oeForm"][passwordStr].value;
        let confirmPassword = document.forms["oeForm"]["confirmPassword"].value;
        if (newPassword != confirmPassword) {
            alert("Passwords don't match");
            return false;
        }
    }
</script>
